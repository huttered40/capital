...


This script will be used to collect data, after the run has finished, from the SCRATCH directory to the

.. actually, this should really just go in the controlScript, as an if statement up top, as there would be a lot of repeated code.
